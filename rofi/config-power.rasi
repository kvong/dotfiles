/*
 * rofi — power menu
 * Compact centered panel for power actions
 */

configuration {
    modi:                       "powermenu";
    display-powermenu:          "";
    show-icons:                 true;
}

@import "config-base.rasi"

window {
    width:                        360px;
    location:                     center;
    anchor:                       center;
}

mainbox {
    children:                     [ "listview" ];
}

listview {
    lines:                        5;
    spacing:                      4px;
    padding:                      12px;
}

element {
    padding:                      14px 18px;
    border-radius:                12px;
}

element-icon {
    size:                        24px;
}

element-text {
    padding:                     0px 0px 0px 14px;
}
